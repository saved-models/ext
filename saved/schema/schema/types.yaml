id: http://marine.gov.scot/metadata/saved/schema/types/
name: types
title: >-
  SAVED data model: custom data types
prefixes:
  linkml: https://w3id.org/linkml/
  saved:  http://marine.gov.scot/metadata/saved/schema/

imports:
  - linkml:types
default_prefix: saved
  
types:
  
  LatLonType:
    pattern: "[0-9]+o [0-9]+.[0-9]+[N|W]"
    base:    str
    repr:    str
    uri:     saved:LatLon
